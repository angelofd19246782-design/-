# Server
PORT=3000

# Session (use a long random string in production)
SESSION_SECRET=change-me-to-a-long-random-string

# Default admin user (created on first launch if employees table is empty)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=raduga2024

# Database file location (relative to project root)
DB_PATH=./data/raduga.sqlite
